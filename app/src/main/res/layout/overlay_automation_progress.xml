<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp"
    android:backgroundTint="@color/m3t_automation_progress_bg">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <!-- 标题 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🤖 AI 自动化执行中"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/m3t_automation_progress_title"
            android:layout_marginBottom="6dp" />

        <!-- 进度文本 -->
        <TextView
            android:id="@+id/tvProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="步骤: 0 / 0"
            android:textSize="12sp"
            android:textColor="@color/m3t_automation_progress_text"
            android:layout_marginBottom="4dp" />

        <!-- 进度条 -->
        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="220dp"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="0"
            android:progressTint="@color/m3t_success"
            android:layout_marginBottom="6dp" />

        <!-- 状态文本 -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="220dp"
            android:layout_height="wrap_content"
            android:text="正在初始化..."
            android:textSize="12sp"
            android:textColor="@color/m3t_automation_progress_text_secondary"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginBottom="8dp" />

        <!-- 按钮区域 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <Button
                android:id="@+id/btnPause"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="暂停"
                android:textSize="12sp"
                android:backgroundTint="@color/m3t_warning"
                android:layout_marginEnd="8dp"
                style="@style/Widget.AppCompat.Button.Colored" />

            <Button
                android:id="@+id/btnCancel"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="取消"
                android:textSize="12sp"
                android:backgroundTint="@color/m3t_error"
                style="@style/Widget.AppCompat.Button.Colored" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
